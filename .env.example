# Generate unique secrets with: uv run python scripts/init_local.py
SECRET_KEY=
# Installation key, separate from SECRET_KEY. Never commit it or store it in SQLite.
# Rotation: new key first, then old keys separated by commas.
HEVY_ENCRYPTION_KEYS=
DEBUG=True
ALLOW_SIGNUPS=True
ALLOWED_HOSTS=localhost,127.0.0.1,testserver
HTTPS=False
CSRF_TRUSTED_ORIGINS=
TUXEDO_DATA_DIR=var/private/v020
SYNC_LOCK_STALE_SECONDS=21600
TUXEDO_FITNESS_BACKUP_DIR=
